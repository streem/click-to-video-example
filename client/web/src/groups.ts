// Experts can be organised into groups.
// When a company is created in Streem, the AGENT group is also created by default.
// Self-defined groups (e.g., PLUMBING) can be created too if the feature is enabled.
// This example uses the AGENT group by default.
export const enum GroupName {
    DEFAULT = "AGENT",
}